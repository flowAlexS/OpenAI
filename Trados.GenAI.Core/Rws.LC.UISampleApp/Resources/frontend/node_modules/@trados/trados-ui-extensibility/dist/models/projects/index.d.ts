/** The type describing the default tabs in the Trados projects list view. */
export type ProjectsListTab = "projects" | "projectGroups";
/** The type describing the default tabs in the Trados project details view. */
export type ProjectDetailsTab = "dashboard" | "stages" | "workflow" | "reports" | "costs" | "files" | "tasksHistory" | "settings" | "notes";
